GameClasses.SkillData = class SkillData {
    constructor(skillType, skillLevel, skillDamage) {
        this.skillType = skillType
        this.skillLevel = skillLevel
        this.skillDamage = skillDamage
    }
}